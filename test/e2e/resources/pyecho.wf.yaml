apiVersion: 1
output: second
tasks:
  first:
    run: pyecho
    inputs:
      body: "{ param('body') }"
  second:
    run: pyecho
    inputs:
      body: "{ output('first') + ', Bye Python' }"
    requires:
    - first